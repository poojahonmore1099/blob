import { ConvertRupeePipe } from './convert-rupee.pipe';

describe('ConvertRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
